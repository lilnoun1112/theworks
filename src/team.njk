---
title: Harmonic Coherence - The Works
layout: 'base.njk'
---

<script src="../js/boxheight.js"></script>
<div class="hero team">
    <div class="textbox-container">
        <div class="titlebox"><h1>The WRI Team</h1></div>
        <div class="textbox"><p>The Works Research Institute collaborates with researchers and experts committed to advancing knowledge and innovation through an interdisciplinary approach.</p></div>
    </div>
    <img class="teaser-3" style="position: absolute; left: 0; right: 0; bottom: 20px; margin-left: auto; margin-right: auto; width: 34px;" src="/images/arrow.png">
</div>

<div class="section-wrapper">
    <div class="publications-container">
        {% set rowCount = 0 %}
        {% for post in collections.team %}
            {% if rowCount % 5 == 0 %}
                {% if rowCount > 0 %}
                    </ul>
                {% endif %}
                <ul class="teaser-container blog">
            {% endif %}
            
            <li class="team-card">
                <article class="teaser-card">
                    <div class="teaser-image" style="background-image: url('/static/{{ post.data.image }}'); background-size: cover"></div>
                    <div class="teaser-textbox">
                        <div class="teas-txt-inner">
                            <h3>{{ post.data.name }}</h3>
                            <h4>{{ post.data.position }}</h4>
                            <p>{{ post.data.field }}</p>
                            <p>{{ post.data.affiliation }}</p>
                        </div>
                        <div class="cvcontainer">
                            {% if post.data.cvlink %}
                                <a class="link" href="{{ post.data.cvlink }}">view cv<div class="href-arrow"></div></a>
                            {% endif %}
                            {% if post.data.cvfile %}
                                <a class="link" href="{{ post.data.cvfile }}">download cv<div class="href-arrow"></div></a>
                            {% endif %}
                        </div>
                    </div>
                </article>
            </li>
            
            {% set rowCount = rowCount + 1 %}
        {% endfor %}
        
        </ul> <!-- Close the last ul if there are any posts -->
    </div>
</div>
