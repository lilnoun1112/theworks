{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "dist"
        }
      }
    ],
    "scripts": {
      "build": "chmod +x ./node_modules/.bin/eleventy && npm run build"
    }
  }
  